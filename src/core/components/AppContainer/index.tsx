import styled from 'styled-components';

import * as vars from '@/core/theme/vars';

const AppContainer = styled.div`
  max-width: ${vars.ApplicationWidth};
  margin: 0 auto;
`;

export { AppContainer };
